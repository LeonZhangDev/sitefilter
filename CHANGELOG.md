# 更新日志

本文件由 `make_package.py --bump` 自动追加，也可以在发布前手工补充说明。

## [Unreleased]

> 下一条变更记在这里，发布时整段改名成版本号。

## [1.3.1] - 2026-09-24

> **补丁版**：修掉两条「本地门禁全绿、用户侧却坏」的缺陷。两条都改到了已发布的 1.3.0 包
> 里的内容，所以单独切一个补丁版，而不是等到下一个次版本。

### 修复

- **Tier B（指定下载器）的本机桥根本不在发布包里**：`make_package.py` 的 `INCLUDE_DIRS`
  只有 `icons`，而排除规则 `EXCLUDE_RE` 又滤掉所有 `.py` / `.md` —— 两件事叠起来，打出来的
  zip 里没有 `native-host/`，而设置页写着「请先运行 `native-host/install.py`」。从 zip 装的
  用户找不到那个文件，**Tier B 对他们实际不可用**，而本地门禁全绿（该目录不被 manifest
  引用，没有任何检查会碰到它）。现在 `native-host/` 进目录白名单，`host.py` / `install.py` /
  `README.md` 显式放行；同时把「会不会进包」收成**唯一**判据 `is_packable()`（打包 / 打包前
  校验 / 打包后扫 zip 三处共用 —— 之前是三处各写各的过滤条件，正是这类漏配的温床）。
  zip 条目 20 → 23。

- **正文里的「隐形空白」会把磁力链接截断，而截出来的半截 hash 还被当合法磁力收下**：
  `MAGNET_RE` 用 `URL_STOP = [^\s"'<>）)】\]]+` 作边界，而 JS 的 `\s` **包含**
  U+FEFF（复制粘贴带出的 BOM 型字符）、U+00A0（`&nbsp;`）、U+3000（全角空格）、U+2009 ——
  站点恰恰常拿它们塞进 infohash 中间做反抓取。`parseMagnet` 只要求「hash 非空」，于是
  `magnet:?xt=urn:btih:c12fe1aa`（8 位）会成为列表里的一条：用户看到一条**点开下不动**的
  链接，页面上没有任何提示（比「少一条」更坏，因为它看起来是成功的）。
  修法（`magnet-core.js::probeBodyMagnets`）：**只在严格候选解析出的 hash 长度不像有效
  infohash**（v1 需 40 hex 或 32 base32，v2 需 ≥64 位偶数 hex）时才试着跨过隐形空白把尾巴
  接回来，且接出的候选必须自身也解析出像样的 hash 才采用 —— 于是
  `链接 + &nbsp; + 另一条链接` 不会被粘成一条，`链接 + &nbsp; + 正文` 不会把正文吃进 hash，
  「被真空格拆开的残串」不猜，本来就能解析的链接行为一字不变。
  同一缺陷的另一个面一并堵上：被接走的尾巴若留在正文里，会被 base64 / 裸 hash 扫描再当成
  一个独立的「32 位裸 hash」收下（一条链接变成两条），所以后续扫描改用「已认领区间挖空」
  的文本。

### 测试

- `_test_magnet.js` 新增 29 项：截断修复的正面（U+FEFF / U+00A0 / U+3000 / U+2009 逐个过）、
  负面（不吃正文、不粘两条链接、被真空格拆开的不猜）、判据（各算法长度口径）、挖空；
  含一条**前提**断言「严格正则确实会在此截断」—— 它保证这一节不会随实现静默失效。
  另补一条端到端：页面里用 `&nbsp;` / U+FEFF 打断 infohash，面板必须给出完整 40 位。
- `_test_native_host.py` 新增 6 项打包范围断言：直接调 `make_package.collect()` 复核
  `native-host/` 三个文件会进包，且例外没被放大（测试 / 打包脚本 / 根文档仍不进包；
  别处的 `.py` 照样被排除）。
- `_test_assembly.js` 新增 3 项静态守卫：Tier B 文件在显式放行名单里、`native-host` 在目录
  白名单里、打包脚本只有一个「会不会进包」的判据（`EXCLUDE_RE.search(` 只应出现一次）。
- `_test_docs.js` 把这两条旧结论（「zip 只有 20 个条目」「被 L2 残缺串丢弃」）加进黑名单。

## [1.3.0] - 2026-09-23

> **本次发布要点**：磁力链接做到「全字段解析 + 反混淆 + 交给本机下载工具」三件事；
> 被屏蔽的卡片可以一键看回；规则体检与影响面预演收成同一套匹配口径；番号补足的放行名单
> 收口到站点表并加去重护栏；修掉 v1.2.0 三个新站写错的选择器（其中一个会把导航菜单认成卡片）；
> content script 拆出两个纯模块（`magnet-core.js` / `site-templates.js`）；MIT LICENSE 入库。
> 以下是逐条记录。

### 修复

- **下载链接探测：磁力链接的 4 个静默错误**（都不会报错，只会让用户"少看到东西"或"看到错的东西"）：
  - **参数顺序敏感**：旧判据 `/^magnet:\?xt=urn:btih:/` 锚定了 `xt=` 必须紧跟 `magnet:?`。
    真实的磁力串里参数顺序任意，`dn=` / `tr=` 排在 `xt=` 前面很常见（不少站的「复制磁力」
    按钮产出的就是这个顺序）—— 这些磁力**整条被丢弃**，页面上一条都看不到。
  - **BitTorrent v2 被丢**：只认 `xt=urn:btih:`，`xt=urn:btmh:`（v2 种子，multihash）
    一律丢弃。现在两种都认，列表里 v2 种子带 `V2` 标记。
  - **去重口径错**：按整串 `raw` 去重，同一种子换个 `tr=` 或换个 `dn=` 顺序就算作两条 ——
    列表里同一部片重复出现。现在按 `infohash` 归并：`tr=` 取并集、文件名取更完整的那个。
  - **体积靠猜**：从不读链接自带的 `xl=`（精确字节数），而是拿外层 DOM 文本做正则匹配 ——
    抓到广告位或相邻内容的体积时是静默的。现在 `xl=` 优先，其次 `size=`/`length=`/`fsize=`，
    最后才从文件名（`...1080p.3.2GB.mkv`）里猜，且只认「数字+单位」的独立词，
    不会把番号里的数字当体积。
- **下载链接排序改为按信息完整度分层**：旧版只做「有体积的靠前」的单级排序，
  同层内顺序随机。现在磁力优先于电驴/迅雷/种子/网盘，同类型内
  「体积+画质 > 体积 > 画质 > 其余」，且同层内保持页面出现顺序（稳定排序）。

### 治理

- **`LICENSE`（MIT）已正式提交**：公开仓库此前无许可证文件，默认「保留所有权利」。
  作者确认口径为 MIT，`LICENSE`（Copyright (c) 2026 Leon Zhang）已随本次提交进入仓库，
  README 末尾也加了「许可证」一节。

### 重构

- **抽出 `magnet-core.js`：磁力/下载链接的解析层独立成模块**（`content.js` 4866 → 4632 行）。
  搬走的是「链接串 ⇒ 结构化数据」这一层：L2 全字段解析（`xl=` 优先于外层 DOM 文本）、
  L3 反混淆（零宽/实体/百分号/base64/裸 hash 五种形态）、体积与画质格式化、同 infohash
  归并（`mergeMagnet`）与 L4 分层排序。边界有意划在「纯」上：扫 DOM、装配结果集、
  页面打标记（`probeLinks`）仍留在 `content.js`；模块只依赖 URL 与 `atob`，
  不碰 `document` / `chrome.storage` / UI 状态，因此可被 Node 直接 `require` 单测。
  搬迁用脚本按锚点原样切片并逐字节校验 —— 磁力正则里含**字面零宽字符**
  （2×U+200B / 4×U+FEFF），手抄必丢，而丢了只表现为「某些站解不出来」的静默失效。
- **抽出 `site-templates.js`：站点表与全部派生只留一份**（`content.js` 4632 → 4511 行）。
  过去站点信息散在 5 处 —— `content.js` 的 `SITE_TEMPLATES` / `DEFAULT_SITES` /
  `KNOWN_SELECTORS` / `SELECTOR_TEMPLATES` / `CODE_SITES`，`background.js` 的 `DEFAULT_SITES`，
  `options.js` 的 `DEFAULT_SITES_OPTIONS` / `TPL_SELECTORS`。加一个站要改 4 个地方，
  漏一处只表现为「面板能开、卡片一个都识别不出」；更隐蔽的是三份 `migrate` 的 v5
  「按 id 补站」名单一旦不同，同一份数据会在不同入口被解读成两种行为。
  现在表 + 全部派生都在 `site-templates.js`，三端各按自己的方式加载同一份：
  `content.js`（manifest 的 content_scripts）、`background.js`（`importScripts`）、
  `options.js`（`options.html` 的 `<script>`）。`SCOPE_OF`（维度 → 默认作用范围）
  也收口到同一份，`content.js` 与 `options.js` 不再各写一遍。
- **依赖 `location` 的逻辑刻意留在 `content.js`**：`templateForHost()` / `linkKindsFor()` /
  裸域兜底 / 按 host 缓存若放进 service worker，会拿 worker 自己的 `location` 去匹配页面 ——
  那是个很难查的错。模块因此保持「纯数据 + 纯派生」，可在 Node 里 `require` 单测。
- **测试装载层补上 service worker 版本**：`_load.js` 新增 `backgroundBundle()`
  （= `importScripts` 目标 + `background.js`，即 service worker 真实的加载结果），
  6 个执行 `background.js` 的测例改走它，不再各自 `readFileSync('background.js')`。
  三个入口都加了显式失败守卫（少了 `SF_SITES` 直接抛错，不静默降级成「什么都不识别」）。
- **`_test_sites.js` 第 ⑤ 节换了守的东西**：从「三处副本必须一致」改为「三端只此一份」——
  源码守卫（三个入口都不得再出现 `id: 's_xxx'` 字面量）+ 派生不变量（`DEFAULT_SITES` /
  `SELECTOR_TEMPLATES` / `CODE_SITES` / `MIRROR_GROUPS` / `KNOWN_SELECTORS` 必须与表里的字段
  严格一一对应）。`_test_assembly.js` 同款增加「不得绕过 `backgroundBundle()`」守卫，
  并补上 `site-templates.js` 的 require 与顺序断言。
- 门禁：**22 套 / 1141 项断言全绿**（此前 README 记的 21 套 / 1116 一直漏计 `_test_assembly.js`，
  本次一并校正）。

### 新增（磁力深度 L3：反混淆）

- **把藏起来的磁力解出来**：很多站故意给磁力做混淆防爬，新版本统一「解一层」再展示，
  覆盖五种常见形态（都是「静默漏掉」——用户根本看不到那条磁力）：
  - **零宽字符注入**（`\u200b` 之类防正则）→ 先剥离不可见字符；
  - **HTML 实体编码**（`magnet:&#x3F;xt=...`、`&amp;`）→ 实体解码；
  - **百分号编码**（`magnet%3A%3Fxt%3D...`）→ 百分号解码；
  - **base64 包一层**（常见藏法：`data-magnet="bWFnbmV0Oj8..."`，点击时 `atob`）→ 解出；
  - **裸 40/32 位 hash 不带 `magnet:` 前缀**（如 `data-hash="a0b1…c8"`）→ 补全 `magnet:?xt=urn:btih:`。
- 解码在 `probeLinks()` 的**三处探测源**（`<a href>` / `data-*` 属性 / 正文文本）统一走
  `decodeObfuscated()`，再由 `dispatchLink()` 按「解码后的真实前缀」重定型别 —— 解码失败不强行当磁力推，
  普通散文不会误报。正文额外新增「百分号编码磁力串」与「base64 token」扫描。
- 为可测，`decodeObfuscated` 也挂在只读测试钩子上；`_test_magnet.js` 新增 11 项 L3 断言
  （直测五种形态 + 端到端：属性藏 base64 / 裸 hash、正文藏零宽字符，面板均出现）。

### 新增（磁力交给本机下载工具）

- **每一行磁力多一个「打开」按钮**：点一下就把 `magnet:` 交给**系统默认的 magnet 协议处理程序**
  （迅雷 / μTorrent / qBittorrent 等），由它去完成真正的 P2P 下载 —— 扩展本身不下载磁力。
  顶部还有「用下载工具打开全部磁力」一键唤起本页所有磁力。
- **设置项「磁力行操作方式」**（通用设置页，三档，默认 `copy` 零行为变化）：
  - `copy`：仅复制（和以前一样，最保守）；
  - `open`：每行只显示「打开」按钮，点一下直接唤起下载工具；
  - `both`：复制 + 打开两个按钮都显示。
- **未装客户端的兜底提示**：扩展无法从扩展侧探测本机是否装了 BT 客户端，所以点「打开」后若没反应，
  面板会给一次提示「请安装迅雷/μTorrent/qBittorrent 并设为系统默认 magnet 处理程序」。
  想「指定用迅雷」就把迅雷设为系统默认 magnet 处理程序（扩展只能唤起系统默认，不能越过系统指定 exe）。
- 实现只造一个隐形 `<a href="magnet:">` 并 `click()`，不需要任何新权限；`DEFAULT_SETTINGS` 三处
  （content/background/options）同步加了 `magnetAction`。`_test_magnet.js` 新增 8 项断言
  （both 模式按钮出现 + 点击生成 magnet 锚点 + 一次性提示；open 模式仅「打开」无「复制」；默认 copy 无「打开」）。

### 新增（磁力交给「指定」下载器：Tier B，可选）

- 上面那版只能唤起**系统默认**那一个 magnet 处理程序。想「指定用迅雷」而不是 μTorrent，
  就得让本机一个小程序去启动那个 exe —— 这就是 Tier B，**按需启用，不开也不影响任何东西**：
  - 设置项「指定下载器路径」：留空 = 走 Tier A（系统默认，零成本）；填了 = 扩展把磁力发给
    本机程序，由它启动你指定的客户端。
  - 新增 `magnet-native.js`（Native Messaging 桥，由 `background.js` 用 `importScripts` 加载）
    + `native-host/`（`host.py` 本机程序 + `install.py` 一次性注册脚本 + 说明）。
  - **安全边界写在本机程序里**：只接受 `magnet:?xt=urn:btih:|urn:btmh:` 且长度受限的串；
    只放行 `open-magnet` 一个动作；`client` 必须是绝对路径、必须在可执行扩展名白名单内
    （`.exe`/`.cmd`/`.bat`），非绝对路径或其它扩展名一律拒绝。请求经 `subprocess` 以**参数数组**
    传参，不走 shell，杜绝注入。
  - `_test_native_host.py`（25 项，纯标准库）把上述边界全部钉死；`_test_magnet_bridge.js`（24 项）
    钉住桥的端口复用、超时分类、迟到/串台响应丢弃与「本机桥失败则自动降级到系统默认」。

### 新增（隐藏可回溯：一键看回被屏蔽的卡片）

- 面板底部新增 **「显示被隐藏（N）」**：点开后本页被屏蔽的卡片**重新出现**（虚线描边 + 半透明，
  一眼看清「原本有什么、为什么被屏蔽」），按钮变成「恢复隐藏」，再点即回到屏蔽态。
- 这是**纯视图态**：不改规则、不写存储；`stats` 里的屏蔽计数也不变
  （所以「揭示」只是让你看一眼，不会让屏蔽失效或污染统计）。
  硬屏蔽（从页面移除）与软屏蔽（灰化遮罩）两种模式都支持回溯。

### 修复（打包漏文件 —— 会导致上架包直接坏掉）

- **`rulecheck.js` 被 `manifest.json` 引用、却不在打包白名单里**，构建出的 zip 缺这个文件，
  装上去内容脚本直接报错。同样的坑对新的 `magnet-native.js` 也成立。两者都已加入白名单，
  并**新增门禁校验**：扫 `manifest.json` 的 `content_scripts` / `web_accessible_resources` /
  `action.default_popup` / `options_page` 与 `background.js` 的 `importScripts(...)` 里引用的本地
  文件，凡是没被白名单覆盖的**直接判失败**（已反证：把任一文件从白名单拿掉，门禁立刻变红），
  以后不会再悄悄漏。
  另在 `_test_collector_native.js` 里补了两项**测试级**防线：直接读 `manifest.json` +
  `background.js` 与 `make_package.py` 的白名单比对，断言「引用到的本地脚本都在名单里」。
  门禁逻辑与测试双保险，任一处被改坏都会红。
- **`_test_collector_native.js` 的 `importScripts` 桩硬编码只认 `collector-native.js`**，
  一旦 `background.js` 多挂一个模块（如 `magnet-native.js`）就在**启动期抛异常**，
  后面的断言全部不执行 —— 而且它**不计入失败计数**（显示「通过 32 / 失败 0」却染红门禁），
  极难查。已改成按名从磁盘加载同目录 `.js`，顺带把原本跑不到的 5 项断言解锁（32 → 37）。

### 加固（数量补足的放行名单与去重护栏）

- **放行名单从硬编码主机名改为站点模板的 `bf` 标记**：原来「哪些站允许补足」写死在
  `BACKFILL_HOST_OK = /javdb580/i` 里，与站点表是两份事实来源。现在由 `SITE_TEMPLATES`
  的 `bf: true` 声明，取值走 `templateForHost()` —— 与卡片识别同一套口径，不会再漂移。
  目前**只有 JavDB580 打标**：这是 2026-09-22 真机实测的结论，不是保守选择 ——
  `javdb.com` 返回 HTTP 200 但只有 1278B（Cloudflare 挑战 + 地区版权封锁）、
  `javdb571.com` HTTP 000（连不上）、`javbus.com` 302 到年龄门，**三个替代源全都拿不到列表页**。
  因此「番号补足多源」这条建议经实测**不可行**，不做（加进去只会每次失败，还白敲人家的站）。
  新增断言把这三个站钉死在「零请求」，防止以后有人凭"同源同 DOM"的直觉把它们放进来。
- **新增去重护栏 + 空页提前止损**：补足是拿下一页的卡片插进当前列表，一旦翻页失效
  （站点改了分页参数，或反爬拦截页伪装成 200 把第 1 页又吐回来），失败模式是**把同一批
  卡片重复贴进列表** —— 用户看到重复内容，且不会有任何报错。现在按卡片详情链接的 pathname
  建指纹，与已有卡片重合的一律不插；整页都是旧内容时**立刻收手**（不再把 3 页配额全敲一遍）。
  正向场景不受影响（有新内容照补）。

### 深化（规则体检 / 影响面预演）

- **把规则匹配口径收口到单一模块 `rulecheck.js`**：`matchEntity(rule, name, scope)` 与
  `ruleImpact(rule, discovered)` 只在此定义，options 页的「规则体检」(`ruleImpact`) 与
  「影响面预演」(`simHits`) 都改用它。此前两者各写一套，`影响面预演` 正确识别了
  `match`(exact/regex)/`aliases`/`scope`，而`规则体检` 用 `indexOf` 子串、把后三者全忽略 ——
  一条正则/精确/别名规则在体检里会被**误判为死规则**（命中 0），与预演结果打架。
- **修了一个真实的匹配 bug（顺带）**：`content.js::matchRule` 只把规则值转小写、**没把卡片文本转小写**，
  导致拉丁字母规则（如 `Mikami`）匹配不到卡片里的 `Mikami Yua`（首字母大写）—— 静默漏匹配。
  现在 hay 也 `toLowerCase()`，与 `simHits` / `matchEntity` 口径统一。这一步靠
  `_test_rulecheck.js` 用 `window.__sfHook.matchRule` 与 `rulecheck.js.matchEntity` 做
  **一致性交叉验证**才暴露，两个函数对 9 组用例必须同进同出。
- **新增「语料库死规则」检测器**：发现库是该用户**真实历史**语义下最贴近离线语料的样本。
  一条规则若 `type` 在发现库里**有同类条目、却一条都不命中**，且它不是 `expr/code/keyword`
  这类发现库不含该维度的规则 → 标记为「疑似无效（语料库无一命中）」，比原来的「零命中」更准：
  原来有命中历史但新样本没命中的也能识别，且不再误伤 `code`/`keyword` 规则。
- `rulecheck.js` 经 `options.html` 的 `<script>` 与 `manifest.json` 的 content_scripts 共同引入，
  不新增权限。`_test_rulecheck.js`（17 项）单测 `matchEntity` 含 exact/regex/contains/aliases/scope，
  并交叉验证 `matchRule` 与之完全一致。门禁 19 套 / 1040 断言（原 18 套 / 1023，+17）。

### 新增功能

- **磁力链接完整字段解析**：`parseMagnet()` 解析 `xt`（v1/v2 + hash 长度）、`dn`、
  全部 `tr`（去重）、`xl`；列表里额外展示 tracker 条数与 v2 标记。
  归并后「复制」出去的是信息最全的那一条（含全部 tracker）。
- **新增测试钩子（只读）**：`content.js` 末尾在 `window.__siteFilterTestApi === 'magnet-only'`
  时挂载 `window.__sfHook`，以只读 getter 暴露纯函数（`parseMagnet` / `fmtBytes` /
  `guessFromName` / `magnetRank` / `buildMagnetRaw`）与只读快照。
  这是为了让磁力解析的**字段口径、归并策略、排序层级**可被直接断言 ——
  此前这些细节只能通过面板文字间接观察，改坏了测试不会红。
  必须在 `window` 上显式声明开关才挂载，且不接受任意对象，避免页面脚本篡改扩展行为。
- 新增 `_test_magnet.js`（64 项断言）：解析器直测 + 面板端到端双层覆盖，钉住 L2/L4 的 4 个回归与 L3 的 5 种反混淆形态。

- **修正 YouPorn 与 xsijishe 的卡片主选择器**（真实页面验证后发现的静默失效）：
  - YouPorn：`tpl` 原为 `li.videoBox`，在真实搜索页**命中 0**。真实卡片是
    `<article class="video-box pc js_video-box js-pop">` —— 标签名是 `article` 不是 `li`，
    class 是 `video-box`（连字符）不是 `videoBox`（驼峰）。已改为 `article.video-box`（实测命中 32~33）。
  - xsijishe：`tpl` 原为 `#threadlist tbody tr`，在真实版块页**只命中 1 个**（那是工具栏行）。
    该站是 Discuz! X3.4 + `nex_*` 主题，帖子行是**纯 `div`**（`id=normalthread_xxx` /
    `stickthread_xxx`），整页 `<tbody>` 只有 1 个。已改为
    `#threadlist div[id^="normalthread_"], #threadlist div[id^="stickthread_"]`（实测命中 36）。
  - 两处 `content.js` 与 `options.js` 的 `TPL_SELECTORS` 副本已同步（`_test_sites.js` 断言两边一致）。
- **堵住论坛「行模式」兜底把导航菜单当帖子行的漏洞**：xsijishe 版块页里有 103 个
  `<ul><li>` 是下拉菜单（"立即注册""图片区"…），旧版 `detectRows()` 的 `'ul li'` 兜底会把它们
  识别成卡片 —— 表现为**用户能"屏蔽"菜单项，真正的帖子一行都屏蔽不到**（静默错误）。
  现在兜底命中必须落在内容容器（`#threadlist` / `#threadlisttableid` / `form#moderate` /
  `.bm` / `#ct`）内，否则换下一个选择器；无明确列表容器的通用论坛维持原逻辑。

### 新增功能

- XChina 详情页可将相册全部图片/视频或单个视频交给 Universal Web Collector，并恢复同内容的最新任务摘要。
- 增加已完成任务的二次重下确认、失败/暂停任务引导、缺失桥接安装提示和用户显式启动的 WSLg 登录恢复。
- Collector 成功、部分完成、失败和取消终态通知均会去重，服务工作者重启不会重复通知。
- 任务页面在活动状态下会单飞轮询最新进度，终态或离开页面后立即停止；本机输出路径不再写入网页 DOM。



## [1.2.0] - 2026-09-22

### 新增功能

- **新增 3 个预置监听站点**：PornHub（`*://*.pornhub.com/*`）、YouPorn（`*://*.youporn.com/*`）、
  西斯寂舍（`*://*.xsijishe.net/*`）。预置站点由 5 个增加到 8 个。三者都带各自的**维度识别规则**，
  卡片右键的「屏蔽演员 / 片商 / 标签」可直接用：
  - PornHub / YouPorn：`/pornstar/`、`/channels/`、`/categories/` 之类的链接与 class 分别归到演员 / 片商 / 标签。
  - 西斯寂舍：Discuz 论坛，**列表行里没有图片**，旧版通用探测（要求 `<a>` 内含 `<img>`）会一张卡都认不出来。
    新增**行模式**（`#threadlist tbody tr`，可见高度阈值 14px 而非 60px）才认得到；`forum-` 前缀归标签、
    `space-uid-` 归发帖人。
  - 这三个站都不是番号站，已显式关闭番号猜测，避免把 `thread-12345`、`ph5f3c...` 之类的 ID 误认成番号。
- **裸域名也能匹配**：`*://*.xsijishe.net/*` 这类通配式配不到 `https://xsijishe.net/`（不带 www）。
  `matchSite()` 增加裸域名兜底，所有预置站点同时受益。
- **站点模板单源化**：原先新增一个站点要同时改 `DEFAULT_SITES`、`KNOWN_SELECTORS`、`SELECTOR_TEMPLATES`、
  `LINK_KINDS`、`CODE_SITES` 五处，漏一处就静默失效（选择器不生效 / 维度全丢）。现在统一收敛到一张
  `SITE_TEMPLATES` 表，其余四处全部由它派生。新增 `_test_sites.js`（47 项）机械守卫这层派生关系，
  以及 content / background / options 三份站点表与版本号的一致性。
- **悬浮球位置可锁定**：锁定后拖不动（防误挪），点击仍能开合面板。三个入口都能切——
  面板标题栏的 🔓/🔒 按钮、**Alt+L**、设置页开关「锁定悬浮球位置」。锁定时球体右下角挂一枚小锁，
  面板底部提示同步变成「Alt+L 解锁球」。位置会记住。
- **窗口缩小后悬浮球不再飘出屏幕**：位置显示前统一做边界收敛（原逻辑只在拖动时夹取，
  缩小窗口会把球留在可视区外）。

### 外观

- **悬浮球重做**：径向渐变球体 + 内高光 + 外圈光晕，48px；hover 放大并亮起光环，按下回弹，
  拖动时放大加粗光环。主色跟随设置里的「高亮色」，球和卡片高亮描边是同一套颜色。
- 面板内按钮（开关胶囊 / 标签页 / 快速屏蔽 / 底部操作 / 筛选）统一加了按下回弹与键盘聚焦环。
  标题栏按钮改成 26px 方块，锁定时变琥珀色。
- 新增 `docs/ball-preview.html`：悬浮球各状态的静态预览页（纯预览，不参与打包）。

- **跨站内容身份（cid）**：给每个卡片一个稳定的内容 ID。
  番号站的 cid 就是番号本身（老数据的已看 / 收藏 / 待看不用迁移，直接继续生效）；
  非番号站没有番号，就从链接里抽站点自己的 ID 并加前缀防撞号 ——
  PornHub 的 `viewkey` → `ph:xxx`、YouPorn 的 `/watch/123` → `yp:123`、Discuz 的 `thread-123` → `dz:123`。
  **已看 / 番号收藏 / 待看队列 / 临时放行 / 详情页自动已看**统一改按 cid 记录，
  所以在 PornHub、YouPorn、西斯寂舍这类站上，♥ 收藏、⏳ 待看、软屏蔽放行现在也能用了
  （以前这些按钮因为没有番号根本不渲染）。抽不出 ID 就留空，不拿垃圾当主键。

- **JavDB 三个镜像归一成一个来源**：`javdb.com` / `javdb571.com` / `javdb580.com` 现在标为同一个镜像组
  `javdb`。发现库改按**来源组**记账（`sites: { javdb: n, javbus: n }`），条目的 `site` 也写组名。
  以前同一个人/标签在三个镜像上各逛一次会被当成三个来源、出现次数 `n` 三倍膨胀，
  进而把规则体检的「命中面」估算和候选规则的覆盖率一起带偏。
  注意：`n`（出现次数）的含义没变，只是多了一份按组拆的账 —— 下游逻辑不用改。

### 工程

- 数据结构版本 **v4 → v5**：迁移步骤 5 会按 id 回填缺失的预置站点，**不动**用户已改过的 pattern / 启用状态，
  且可重复执行。三处（content / background / options）同步升级。
- 新增守卫：三处 `DEFAULT_SETTINGS` 的关键字段与三处 `DEFAULT_KEYS` 键名必须一致
  （漏改一处的表现是设置页开关不显示、或快捷键被 `normKeys` 静默回落成默认值）。
- 门禁合计 **13 套 / 691 项断言**（`_test_sites.js` 扩到 67 项，`_test_migrate.js` 扩到 94 项）。

## [1.1.0] - 2026-09-20

### 新增功能

- **规则体检**（规则管理页）：把「为什么这条规则一直不生效」讲清楚。对启用超过 7 天却 0 命中的规则，按
  「规则关闭 / 所属分组关闭 / 临时规则已到期 / 限定站点没刷到 / 匹配方式选了『精确』/ 作用范围不对 /
  条件表达式为假 / 评分与日期字段没解析出来」逐条给出原因；同时列出**命中面过宽**的屏蔽规则
  （发现库里超过 70% 的同类条目都带这个词，例如「高清」「中文字幕」），提示改成标题词或限定作用范围。
  默认不勾选任何待删项，避免一键误删。
- **候选规则（自动学习）**：后台每日归纳「你屏蔽的人身上有哪些共同标签 / 片商 / 系列」并给出建议。
  判定用 **覆盖率 × 精确率** 双指标，两者都够高才推 —— 只看覆盖率会推出「高清」这种没意义的词。
  每条建议显示覆盖率、精确率、得分与证据，可一键**采纳**（直接建成规则，带溯源信息）或**忽略**。
- **场景档位**（分组页）：把「每条规则的启用状态 + 每个分组的开关 + 几个关键行为开关」存成一套档位，
  一键来回切换（例如「日常」和「扫片模式」）。档位不含规则内容本身，删规则不会牵连档位。
- **月度回顾**（数据看板）：按自然月汇总屏蔽 / 收藏 / 高亮 / 标记已看四项动作，给出环比、
  本月活跃天数、近 6 个月堆叠柱状图，以及本月仍在命中的规则 Top 5。
- **已看自动记录**：打开详情页时自动把该番号记为「已看」，番号从 URL 路径解析（`/ABC-001`、`/abc001` 都认）。
  可在通用设置里关掉。已记录过的不覆盖原时间戳。
- **规则调试器**：卡片右键 → 「调试这张卡」，浮层列出该卡片的字段摘录、**结论**（哪条规则生效）、
  以及逐条规则的判定过程（每一步通过 / 不通过 + 原因），并标出「已关闭 / 已过期 / 分组关闭 /
  ★ 生效 / 命中但被压过」。它用的是页面里同一份匹配与决策逻辑（`matchRuleDetail` + `decideCard`），
  **面板说什么，页面就做什么**，不是另写一份近似推断。
- **临时规则有效期**：卡片右键提供「临时屏蔽 1 / 7 / 30 天」，规则编辑行可设「有效期至」或「设为永久」。
  到期后后台会真正移除该规则（不是"仍生效但跳过"），并记入 `expiredLog`（上限 200 条）供回查。
  规则表新增「有效期」列，显示剩余时间。
- **加密备份导入导出**：备份可用口令加密后导出，适合放网盘。算法为
  **PBKDF2-SHA256（25 万次迭代）派生密钥 + AES-GCM 256 加密**，盐值与初始向量每次导出都重新随机。
  文件格式自描述（`fmt` / `iter` / `alg` / `salt` / `iv` / `data`），便于以后升级参数。
  导入时自动识别是否加密文件并索要密码。**扩展不保存密码** —— 忘记密码等于文件打不开。
- **建规则前影响面预估**：用发现库估算新屏蔽规则会波及多少条目，命中面过大时先弹确认框
  （可在通用设置里关掉）。

### 工程

- 数据结构版本 **v3 → v4**，迁移步骤覆盖 content / background / options 三处。
- 新增测试套件 `_test_newfeat.js`（41 项）、`_test_encrypt.js`（30 项）；
  `_test_migrate.js` 扩到 80 项（含 v3→v4 迁移、到期清理、**三处版本号一致性守卫**）。
- 门禁合计 **12 套 / 609 项断言**。

### 修复

- **content.js 的 `SCHEMA_VERSION` 未随版本升级**（仍为 3）：内容脚本每次写回都会把数据
  `schemaVersion` 降级回 3，导致新字段在下次读取时被"补空"逻辑覆盖。已修正，并加守卫测试
  断言三个文件的版本号必须一致。
- **background.js 的 v4 迁移漏补 `expiredLog`**：迁移后该字段缺失，后台清理过期规则时依赖它追加日志。

## [1.0.1] - 2026-09-19

- 新增条件表达式规则引擎（expr.js）与可自定义快捷键；打包脚本接入 CI 门禁与版本号自增（版本号自增：patch）
