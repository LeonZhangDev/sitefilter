# 行尾约定（只钉会「静默失效」的那两类，不扩大到全仓库 —— 全仓库重排行尾会制造
# 一次谁也没改内容的巨型 diff）。
#
# ① 钩子脚本必须是 LF：CRLF 的钩子**不报错，只是不生效** —— sh 会把 `\r` 读进变量，
#    于是 `[ "$code" = "0" ]` 永不成立：pre-push 要么永远放行、要么永远拦截。
#    两种都极难查，因为没人会去读钩子自己的字节。
# ② workflow 同理：YAML 里的 CR 会让有些步骤名/命令带上一颗看不见的尾字符。
.githooks/* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
